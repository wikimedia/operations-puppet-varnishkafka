[Unit]
Description=VarnishKafka <%= @name %>
After=network.target local-fs.target

[Service]
Type=simple
ExecStart=/usr/bin/varnishkafka -S "/etc/varnishkafka/<%= @name %>.conf"
Restart=on-failure

[Install]
WantedBy=multi-user.target
